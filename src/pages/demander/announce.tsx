import { AnnouncementList } from "@/components/announcement/announce_list";
import React from "react";
const Announce = () => {
  return <AnnouncementList isAdmin={false} />;
};

export default Announce;
